// DOM component
// export function updateState(field, value, stateUpdater) {
//     console.log(`${field} changed to ${value}`); // Log the change

//     //Update the state to re render
//     if (stateUpdater) {
//         stateUpdater((prevState) => ({
//             ...prevState, // Copy all current key-value pairs from the state
//             [field]: value, // Update the specific field
//         }));
//     }
// }
